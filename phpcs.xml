<?xml version="1.0"?>
<ruleset name="Papi Core">
    <description>PHP CodeSniffer rules for Papi Core library</description>

    <!-- Include PSR-12 ruleset -->
    <rule ref="PSR12"/>

    <!-- Files to check -->
    <file>src</file>
    <file>tests</file>

    <!-- Exclude patterns -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/coverage/*</exclude-pattern>

    <!-- Allow descriptive test method names in test files -->
    <rule ref="PSR1.Methods.CamelCapsMethodName">
        <exclude-pattern>*/tests/*</exclude-pattern>
    </rule>

    <!-- Allow multiple classes in test files -->
    <rule ref="PSR1.Classes.ClassDeclaration.MultipleClasses">
        <exclude-pattern>*/tests/*</exclude-pattern>
    </rule>

    <!-- Show progress -->
    <arg value="p"/>

    <!-- Show sniff codes in all reports -->
    <arg value="s"/>

    <!-- Show colors -->
    <arg value="n"/>

    <!-- Show source codes in all reports -->
    <arg value="v"/>
</ruleset> 